pispy
=====

RPi camera module software for webcam

~~`refresh.cgi` Bash CGI script that runs raspistill~~

`apiserver.py` Python/Flask app that returns temperature sensor readings

`prunearchive` Bash script that runs as a hook in motion to clean up old files
